import doctest

from RNA Nucleotide Count import rna_count

def rna_count(dna):
    """ 
    Count the n-th is a rna_count(dna)
    >>> dna('A')
    totalA += 1
    >>> dna('C')
    totalC += 1
    >>> dna('G') 
    totalG += 1
    >>> dna('U')
    totalU += 1
    >>> 
    """

    return True

if __name__ == "__main__": 
    doctest.testmod()
    test Pass
