import doctest

from dna import to_rna

def to_rna(n):
    """ 
     replacing each nucleotide with its complement
    >>> to_rna(G)
    C
    >>> to_rna(C)
    G
    >>> to_rna(T) 
    A
    >>> to_rna(A)
    U
    >>> 
    """

    return True

if __name__ == "__main__": 
    doctest.testmod()
    test Pass
    
